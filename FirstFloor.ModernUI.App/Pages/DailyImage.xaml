<UserControl x:Class="FirstFloor.ModernUI.App.Pages.DailyImage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
        

        <Grid Margin="{StaticResource ContentMargin}">
            <Grid.Resources>
                <ResourceDictionary>
                    <ResourceDictionary.MergedDictionaries>
                        <ResourceDictionary Source="/Assets/Style/DailyImage.xaml" />
                    </ResourceDictionary.MergedDictionaries>
                </ResourceDictionary>
            </Grid.Resources>
            
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- The Image -->
            <mui:ModernProgressRing IsActive="True" Width="80" Height="80" />
            <Image Source="{Binding ImageUri}" />

            <!-- Image Title -->
            <Grid Margin="{StaticResource HeaderMargin}" HorizontalAlignment="Right" VerticalAlignment="Top">
            <TextBlock Text="{Binding ImageTitle}" Style="{StaticResource Heading2}" />
            </Grid>

            <!-- Image Description -->
            <Grid Grid.Row="1" >
                <Rectangle Margin="0,5,0,0"/>
                <TextBlock Text="{Binding ImageDescription}" />
            </Grid>
        </Grid>
</UserControl>
